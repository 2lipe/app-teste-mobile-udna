import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 5,
    marginBottom: 5,
    padding: 15,
    backgroundColor: '#FFF',
    elevation: 1,
    borderRadius: 4,
  },

  first: {
    marginTop: 10,
  },

  last: {
    marginBottom: 10,
  },
});

export default styles;
